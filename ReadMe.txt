Step1.

進 C 槽 -> Project -> src -> 打開 config.js (右鍵 用記事本開啟)

Step2.

將網址放在 url 後 ==> 「url : '放這裡'」 
修改網址特徵在 charater ==> 「charater: '放這裡'」 

Step3.

按開始 -> 搜尋 cmd -> 打開終端機

Step4.

貼上此指令 : node Project/src/getImage.js
(在終端機點右鍵 -> 貼上)


Note:

* 若要截多個網頁的圖 請重新在 config.js 改網址 再回終端機下一次 Step4.指令 即可